# dependencies
node_modules/

# env
.env
.env.local
.env.*.local

# Cloudflare Wrangler — `.dev.vars` はローカル Worker の秘密値
# (DATABASE_URL / SUPABASE_SERVICE_ROLE_KEY) を保持する。絶対に commit しない。
.dev.vars
.dev.vars.local
.wrangler/

# logs
.playwright-mcp/
*.log
npm-debug.log*
pnpm-debug.log*

# build artifacts
dist/
build/
.svelte-kit/
.cache/
.turbo/
.vite/
.vite-hooks/
.pnpm/

# テストカバレッジ (vitest --coverage の出力)
coverage/

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Unity (app/unity/) — Unity が生成する重い成果物は追跡しない
app/unity/Library/
app/unity/Temp/
app/unity/Obj/
app/unity/Build/
app/unity/Builds/
app/unity/Logs/
app/unity/UserSettings/
app/unity/MemoryCaptures/
app/unity/[Bb]ootstrap/
app/unity/*.csproj
app/unity/*.sln
app/unity/*.user
app/unity/*.suo
app/unity/*.userprefs
app/unity/*.pidb
app/unity/*.booproj
app/unity/*.svd
app/unity/*.unityproj
app/unity/sysinfo.txt
# Unity Licensing — Manual Activation File (機密性は低いが追跡不要)
app/unity/*.alf

# server — ローカルアセット (.fbx) 置き場 (将来 R2 移行)
app/server/storage/

# accidental (PowerShell の `2>$null` 取り違えで生まれる)
nul
