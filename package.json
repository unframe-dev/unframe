{
  "name": "unframe",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "vp config",
    "notion:list-pages": "pnpm --filter unframe-notion-sync list-pages",
    "notion:sync": "pnpm --filter unframe-notion-sync sync"
  },
  "devDependencies": {
    "vite-plus": "catalog:",
    "zod": "4.5.4"
  },
  "engines": {
    "node": ">=22"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.14.0",
      "onFail": "download"
    }
  }
}
