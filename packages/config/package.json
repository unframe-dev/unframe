{
  "name": "@unframe/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript and repository tooling configuration",
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "vp check vite.config.ts test",
    "test": "tsx test/pre-commit.fixture.test.ts",
    "check": "pnpm typecheck && pnpm test && pnpm lint"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.22.1",
    "typescript": "^7.0.0",
    "vite-plus": "catalog:"
  }
}
