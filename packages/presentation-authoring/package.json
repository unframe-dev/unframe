{
  "name": "@unframe/presentation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "vp check src test",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm lint && pnpm test"
  },
  "dependencies": {
    "@unframe/presentation-core": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.0",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
